name: Build and test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.15.0
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Run privacy audit
        run: npm run audit:privacy

      - name: Run image search tests
        run: npm test -- --runInBand server/image-search.test.js

      - name: Build
        run: npm run build
